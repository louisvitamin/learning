import pandas as pd

# Now we use K-Mean clustering to cluster the S&P 500 stocks
from sklearn import cluster, datasets

new_cor = dr.corr()
k_means = cluster.KMeans(n_clusters=3)
k_means.fit(new_cor )

sp_k = pd.Series(k_means.labels_, index=new_cor.columns.tolist())

